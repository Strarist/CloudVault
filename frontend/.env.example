# Frontend environment (copy to `.env.local` for local, or set in Vercel Project Settings)
# Never commit real secrets. Backend keys do NOT belong here.

# Local:
# NEXT_PUBLIC_API_URL=http://localhost:3000
#
# Production (Vercel):
# NEXT_PUBLIC_API_URL=https://cloudvault-api.onrender.com

NEXT_PUBLIC_API_URL=http://localhost:3000
